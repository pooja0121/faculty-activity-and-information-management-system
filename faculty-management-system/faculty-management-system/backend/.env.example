# Environment Variables for Faculty Management System Backend

# MongoDB Atlas Connection String
# Replace <username>, <password>, and <database> with your actual values
MONGO_URI=mongodb+srv://<username>:<password>@ac-z6wkaya-shard-00-00.uoglk1o.mongodb.net/<database>?ssl=true&replicaSet=atlas-vu0y0m-shard-0&authSource=admin&retryWrites=true&w=majority

# Server Port (optional, defaults to 5000)
PORT=5000

# JWT Secret for authentication
JWT_SECRET=your_jwt_secret_here

# Email configuration (if using nodemailer)
EMAIL_USER=your_email@gmail.com
EMAIL_PASS=your_email_password
